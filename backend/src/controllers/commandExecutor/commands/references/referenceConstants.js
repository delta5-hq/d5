/**
 * Reference definition prefix (single @)
 */
export const REF_DEF_PREFIX = '@'

/**
 * Reference usage prefix (double @@)
 */
export const REF_PREFIX = '@@'

/**
 * Hashref definition prefix (single #_)
 */
export const HASHREF_DEF_PREFIX = '#_'

/**
 * Hashref usage prefix (double ##_)
 */
export const HASHREF_PREFIX = '##_'
