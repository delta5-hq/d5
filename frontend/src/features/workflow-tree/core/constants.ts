/** Horizontal indent per tree depth level (px) */
export const INDENT_PER_LEVEL = 32

/** Row height in the virtualized tree (px) */
export const ROW_HEIGHT = 48

/** Inset from the wire column center to avoid overlapping the node content (px) */
export const WIRE_PADDING = 2

/** Left padding before the first wire column (px) */
export const BASE_PADDING = 12

/**
 * Duration of the spark-travel CSS animation (ms).
 * Must stay in sync with the `animation` value in wire-tree.css.
 */
export const SPARK_DURATION_MS = 750
