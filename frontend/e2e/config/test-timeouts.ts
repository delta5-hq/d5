/* Centralized timeout constants for E2E tests */

export const TIMEOUTS = {
  SIDEBAR_TRANSITION: 300,
  SIDEBAR_ANIMATION: 500,
  SECTION_SWITCH: 600,
  NAVIGATION: 15000,
  NETWORK_IDLE: 40000,
  RAPID_INTERACTION_DELAY: 100,
  AUTH_REFRESH_RESPONSE: 120000,
  AUTH_STATE_SETTLEMENT: 1000,
  UI_UPDATE: 500,
  BACKEND_SYNC: 10000,
  EXPECT_DEFAULT: 5000,
  AUTO_PUBLISH: 15000,
  SLOW_NETWORK: 20000,
  DIALOG_ANIMATION: 500,
} as const

export type TimeoutKey = keyof typeof TIMEOUTS
