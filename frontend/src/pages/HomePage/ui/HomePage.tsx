import { Main } from '@shared/components/Main'
import { AppLayout } from '@shared/layout/AppLayout'

const HomePage = () => (
  <AppLayout breakpoint={1440}>
    <Main />
  </AppLayout>
)

export default HomePage
